const fs = require('fs');
const path = require('path');

module.exports = function(view){
    return fs.readFileSync([process.cwd(), 'src/view' ,view.replace(/\./g, path.sep) + '.tp'].join(path.sep), 'utf8');
}
