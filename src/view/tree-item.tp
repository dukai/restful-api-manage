<li :class="{active: open}">
  <a 
    :class="{bold: isFolder, active: open, selected: selected}"
    @click="_select" >
    <i 
      v-if="isFolder" 
      @click.stop="toggle"
      class="glyphicon {{open ? 'glyphicon-menu-up' : 'glyphicon-menu-down'}}">
      </i>
    {{model.name}}

    <span class="operate">
      <span class="del" @click.stop="_removeChild">-</span>
      <span @click.stop="_addChild">+</span>
    </span>
  </a>
  <ul v-show="open" v-if="isFolder" class="list">
    <item
      class="item"
      v-for="model in model.children"
      :model="model">
    </item>
  </ul>
</li>
